# c64-koala-recolor

## Requirements

* rust (https://rustup.rs/)

## Running

The program expects four colors as arguments, reads a koala image from stdin
and writes the koala with adapted colors to stdout.

```shell
cargo run -- 0 1 7 10 < input.koa > output.koa
```
